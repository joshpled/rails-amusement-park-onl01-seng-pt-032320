<%= @attraction.name %>
<button> <%= form_for @ride do |f| %>
        <%= f.hidden_field :user_id %><br />
        <%= f.hidden_field :attraction_id %><br />
        <%= f.submit "Go on this ride", class:"btn btn-light" %>
      <% end %></button>

      
      <% if current_user.admin == true %>
        <%= link_to "Edit Attraction", edit_attraction_path%>
      <% end %>
      
  